kind: Service
apiVersion: v1
metadata:
  name: react-service
  namespace: nginx
spec:
  selector:
    app: netflix
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
